<odoo>
    <!--Add the certificat field to the base.view_partner_form after vat(Tax ID) field.-->
    <record id="res_users_view_form" model="ir.ui.view">
        <field name="name">res.partner.view.form.inherit.certificat</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='vat']" position="after">
                <field name="certificat"/>
            </xpath>
        </field>
    </record>
    <!--Add the certificat field to the base.view_partner_tree after the email field.-->
    <record id="res_users_view_tree" model="ir.ui.view">
        <field name="name">res.partner.view.tree.inherit.certificat</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='email']" position="after">
                <field name="certificat"/>
            </xpath>
        </field>
    </record>
</odoo>